name: Clone, Build, and Push Backend Docker Image

on:
  push:
    branches:
      - main1
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout trigger repo (this repo)
      uses: actions/checkout@v4

    - name: Clone backend repository
      run: |
        git clone https://github.com/YOUR_USERNAME/YOUR_BACKEND_REPO.git backend

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ dckr_pat_uTmr8IkEFR22wgEjmNsQP5v-Moc }}

    - name: Build and push backend Docker image
      uses: docker/build-push-action@v5
      with:
        context: ./backend  # Uses the cloned repo as Docker build context
        push: true
        tags: 12mahe/backend:latest
